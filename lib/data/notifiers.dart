//Valuenotifier :hold the data
//ValueListenableBuilder: listen to the data changes(dont need to use setState)

import 'package:flutter/widgets.dart';

ValueNotifier selePageNotifier = ValueNotifier(0);

ValueNotifier isDarkModeNotifier = ValueNotifier(true);